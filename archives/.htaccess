# Deny all direct access to CSV archive files.
# These contain customer PII (names, addresses, phone numbers).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
